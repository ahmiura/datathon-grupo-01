# Pipeline de Governança de Dados (DVC)
# Demonstração para a Banca de Maturidade MLOps (Nível 2/3)

stages:
  generate_synthetic_dev_data:
    cmd: python src/data/generate_synthetic.py
    deps:
      - src/data/generate_synthetic.py
    outs:
      - data/processed/synthetic_data_dev.csv

  # Exemplo de snapshot versionavel derivado da feature store PostgreSQL canonica
  # feature_store_snapshot:
  #   cmd: python -c "from src.features.feature_store import materialize_feature_store_snapshot; from src.config import TICKER, START_DATE, END_DATE; materialize_feature_store_snapshot(TICKER, START_DATE, END_DATE)"
  #   outs:
  #     - data/processed/feature_store.parquet
